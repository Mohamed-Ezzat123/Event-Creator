import 'package:flutter/material.dart';

class HomePackage extends StatelessWidget {
  const HomePackage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
